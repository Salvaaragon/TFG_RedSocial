{% extends 'base_index.html.twig' %}

{% block body %}

{% if errors is defined %}
<section class="register_block register_block_witherrors">
{% else %}
<section class="register_block register_block_withouterrors">
{% endif %}
    <div class="container register_global_panel">
        <div class="col-md-12 register_top_separation"></div>
        <div class="col-md-12 register_top_panel">
            <h2 class="text-center">FORMULARIO DE REGISTRO</h2>
        </div>
        {% if errors is defined %}
        <div class="card text-white bg-danger mb-12 register_error_card">
            <div class="card-header">Se han encontrado los siguientes errores</div>
            <div class="card-body register_error_body">
                <ul class="register_error_list">
                    {% for error in errors %}
                        <li>{{ error.message }}</li>
                    {% endfor %}
                </ul>
            </div> 
        </div>
        {% endif %}               
        
        <div class="row">
            <div class="col-md-6 register_left_panel">
            {{ form_start(form) }}
                <div class="form-group">{{ form_widget(form.username) }} </div>                                
                <div class="form-group">{{ form_widget(form.plainPassword.first) }}</div>
                <div class="form-group">{{ form_widget(form.profileName) }}</div>
                <div class="form-group">{{ form_widget(form.psn_id) }}
            </div></div>
            <div class="col-md-6 register_right_panel">
                <div class="form-group">{{ form_widget(form.email) }}</div>
                <div class="form-group">{{ form_widget(form.plainPassword.second) }}</div>
                <div class="form-group">{{ form_widget(form.steam_id) }}</div>
                <div class="form-group">{{ form_widget(form.xbox_id) }}</div>
            </div>
        <div class="col-md-12">
            {{ form_widget(form.submit) }}
                <button type="button" onclick="window.location.href = '{{ path('index') }}'" class="btn btn-danger btn-block">
                    Volver a la página principal
                </button>
            {{ form_widget(form.reset) }}
        {{ form_end(form) }}
        </div>
        </div>
        <div class="col-md-12 register_bottom_separation"></div>
    </div>
</section>
{% endblock %}